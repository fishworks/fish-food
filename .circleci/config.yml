version: 2
jobs:
  build:
    working_directory: ~/fish-food
    docker:
      - image: ubuntu
    steps:
      - run:
          name: Install Fish
          command: .circleci/install-fish.sh
      - run:
          name: Test
          command: .circleci/test.sh
workflows:
  version: 2
  build:
    jobs:
      - build:
          filters:
            tags:
              only: /.*/
